### GNU Screen Configuration File

### General Configuration Options

# if we accidentally hangup, don't be all attached when we come back. 
autodetach on

# Allow bold colors - necessary for some reason
attrcolor b ".I"
attrcolor i "+b"

# Set the colors for screen messages to default on blue background.
sorendition = md

# Suppress trailing blank lines when scrolling up in the history buffer.
compacthist on

# Enable mouse
defmousetrack on

# Save lots of scrollback.
defscrollback 50000

# Ignore case when searching.
ignorecase on

# Disable the startup splash message.
startup_message off

### Status Line Configuration Options

backtick 1 30 15 uname -r

hardstatus alwayslastline "%{= dd} %-w%{= md} %n %t %{= dd}%+w   %{= md} %H %{= dd} %l %{= md} %Y-%m-%d%C:%s %A %{= dd} %1` %="
